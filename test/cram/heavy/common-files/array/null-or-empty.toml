[project]
files = ["null-or-empty.c"]
entrypoint = "null_or_empty"
criterion = "DC"

[pointer-handling]
array-size-mapping = ["a:n"]
max-ptr-array-length = 2

[klee]
timeout = 5

[libfuzzer]
timeout = 0
runs = 1000
seed = 42
