; generated by drom from package skeleton 'library'

(library
  (name seacoral_basics)
  (public_name seacoral-basics)
  (wrapped false)
  ; use field 'dune-libraries' to add libraries without opam deps
  (libraries terminal_size fmt.tty lwt.unix fmt )
  ; use field 'dune-flags' to set this value
  (flags (:standard))
  ; use field 'dune-stanzas' to add more stanzas here
  
  
  )


(documentation
  (package seacoral-basics))

; use field 'dune-trailer' to add more stuff here
(rule
 (targets format_shims.ml)
 (deps    (:first-dep format_shims.cppo.ml))
 (action  (run %{bin:cppo} -V OCAML:%{ocaml_version} %{first-dep} -o %{targets})))

