; generated by drom from package skeleton 'library'

(library
  (name sc_values)
  (public_name seacoral-values)
  (wrapped true)
  ; use field 'dune-libraries' to add libraries without opam deps
  (libraries seacoral-crunch seacoral-core seacoral-c seacoral-basics ctypes lwt.unix fmt )
  ; use field 'dune-flags' to set this value
  (flags (:standard))
  ; use field 'dune-stanzas' to add more stanzas here
  
  
  )


(documentation
  (package seacoral-values))

; use field 'dune-trailer' to add more stuff here
(rule
 (targets res.ml)
 (deps
  (:gen %{bin:ocaml-crunch})
  (source_tree res))
  (action
   (run %{bin:ocaml-crunch} --silent --mode=plain --output res.ml res))
)
