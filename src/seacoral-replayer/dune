; generated by drom from package skeleton 'library'

(library
  (name sc_replayer)
  (public_name seacoral-replayer)
  (wrapped true)
  ; use field 'dune-libraries' to add libraries without opam deps
  (libraries seacoral-project seacoral-ltest seacoral-lib seacoral-crunch seacoral-core lwt.unix fmt )
  ; use field 'dune-flags' to set this value
  (flags (:standard))
  ; use field 'dune-stanzas' to add more stanzas here
  
  
  )


(documentation
  (package seacoral-replayer))

; use field 'dune-trailer' to add more stuff here
(rule
    (targets resources.ml)
    (deps
        (:gen %{bin:ocaml-crunch})
        (source_tree resources)
    )
    (action
        (run %{bin:ocaml-crunch} --silent --mode=plain -o resources.ml resources)
    )
)
